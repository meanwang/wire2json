// Code generated by Wire protocol buffer compiler, do not edit.
// Source file: .\profile.proto
package com.wesocial.apollo.protocol.protobuf.profile;

import com.squareup.wire.Message;
import com.squareup.wire.ProtoField;
import java.util.Collections;
import java.util.List;
import okio.ByteString;

import static com.squareup.wire.Message.Datatype.BYTES;
import static com.squareup.wire.Message.Label.REPEATED;

/**
 * 目前会返回好友的基础信息以及第一张头像，以及对应的base_version
 */
public final class BatchGetUserInfoRsp extends Message {
  private static final long serialVersionUID = 0L;

  public static final ByteString DEFAULT_RESERVED_BUF = ByteString.EMPTY;
  public static final List<AllUserInfo> DEFAULT_USER_INFO = Collections.emptyList();

  @ProtoField(tag = 1, type = BYTES)
  public final ByteString reserved_buf;

  /**
   * 服务端使用，客户端忽略
   */
  @ProtoField(tag = 2, label = REPEATED, messageType = AllUserInfo.class)
  public final List<AllUserInfo> user_info;

  public BatchGetUserInfoRsp(ByteString reserved_buf, List<AllUserInfo> user_info) {
    this.reserved_buf = reserved_buf;
    this.user_info = immutableCopyOf(user_info);
  }

  private BatchGetUserInfoRsp(Builder builder) {
    this(builder.reserved_buf, builder.user_info);
    setBuilder(builder);
  }

  @Override
  public boolean equals(Object other) {
    if (other == this) return true;
    if (!(other instanceof BatchGetUserInfoRsp)) return false;
    BatchGetUserInfoRsp o = (BatchGetUserInfoRsp) other;
    return equals(reserved_buf, o.reserved_buf)
        && equals(user_info, o.user_info);
  }

  @Override
  public int hashCode() {
    int result = hashCode;
    if (result == 0) {
      result = reserved_buf != null ? reserved_buf.hashCode() : 0;
      result = result * 37 + (user_info != null ? user_info.hashCode() : 1);
      hashCode = result;
    }
    return result;
  }

  public static final class Builder extends Message.Builder<BatchGetUserInfoRsp> {

    public ByteString reserved_buf;
    public List<AllUserInfo> user_info;

    public Builder() {
    }

    public Builder(BatchGetUserInfoRsp message) {
      super(message);
      if (message == null) return;
      this.reserved_buf = message.reserved_buf;
      this.user_info = copyOf(message.user_info);
    }

    public Builder reserved_buf(ByteString reserved_buf) {
      this.reserved_buf = reserved_buf;
      return this;
    }

    /**
     * 服务端使用，客户端忽略
     */
    public Builder user_info(List<AllUserInfo> user_info) {
      this.user_info = checkForNulls(user_info);
      return this;
    }

    @Override
    public BatchGetUserInfoRsp build() {
      return new BatchGetUserInfoRsp(this);
    }
  }
}
